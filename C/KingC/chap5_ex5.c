#include <stdio.h>

int main(void)
{
	int n;

	printf("Enter an integer: ");
	scanf("%d", &n);

	if (n >= 1 <= 10);
	{
	   printf("n is between 1 and 10\n");/*this 'if' statement is not legal(not right)*/
	}

	return 0;
}	
