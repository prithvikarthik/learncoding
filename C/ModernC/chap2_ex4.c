#include <stdio.h>

int main(void)
{
	int x, y, z;
	float p, q, r;

	printf("x = %d, y = %d, z = %d\n", x, y, z);
	printf("p = %f, q = %f, r = %f\n", p, q, r);

	return 0;/*values are changing after each time I ran it*/
}	
